version: '3.8'

services:
  backend:
    build: .
    container_name: k8slab-backend-standalone
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    networks:
      - backend-network

networks:
  backend-network:
    driver: bridge